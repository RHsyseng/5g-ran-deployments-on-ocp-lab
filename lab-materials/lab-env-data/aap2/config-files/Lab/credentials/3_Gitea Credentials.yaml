---
controller_credentials:
  - name: "Gitea Credentials"
    description: ""
    credential_type: "Source Control"
    organization: "Lab"
    inputs:
      password: "{{ gitea_pwd }}"
      username: "{{ gitea_user }}"

...
